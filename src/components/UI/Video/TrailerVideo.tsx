import React, {FC, memo} from 'react';
import cl from './styles/TrailerVideo.module.scss'

interface TrailerVideoProps {

}


const TrailerVideo: FC<TrailerVideoProps> = ({  }) => {
    return (
        <h1>Trailer Video</h1>
    );
};

export default memo(TrailerVideo);