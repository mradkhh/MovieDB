import React, {FC, memo} from 'react';
import cl from './styles/Download.module.scss'

interface DownloadProps {

}


const Download: FC<DownloadProps> = ({  }) => {
    return (
        <h1>Download video</h1>
    );
};

export default memo(Download);